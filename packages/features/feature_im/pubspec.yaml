name: feature_im
description: WeChat/WhatsApp style IM module for Zhixuan Super App
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # 核心依赖：设计系统与网络
  core_design_system:
    path: ../../core/core_design_system
  core_network:
    path: ../../core/core_network
  core_local_db:
    path: ../../core/core_local_db
    
  # IM 特定依赖
  intl: ^0.19.0 # 用于时间格式化

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0